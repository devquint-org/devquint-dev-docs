# 10 Принципов создания планов реализации

**Это обязательное чтение** (5 минут). После прочтения переходи к `CHECKLIST.md` или шаблонам.

---

## 1️⃣ Линейная независимость этапов

**Правило:** Этап N может быть завершён и протестирован **независимо** от этапов N+1, N+2, ...

**Проверка:**
- Может ли team начать этап N+1 после этапа N?
- Может ли быть тест этапа N без этапа N+1?

**Если нет** → план неправильный, перестраивай.

---

## 2️⃣ Разделение слоев (архитектура)

**Правило:** Зависимости указывают только **вниз** или горизонтально. Никогда вверх.

```
API / Controllers
     ↓
Application / Services  
     ↓
Domain / Models
     ↓
Infrastructure / DB
```

**Проверка:** Domain знает о фреймворке? Если да → ошибка.

---

## 3️⃣ Infrastructure на отдельных этапах

**Правило:** ВСЕ shared infrastructure (DB, Auth, Logging) выделены в **отдельные этапы** ДО использования.

**Типичная ошибка:**
```
❌ Этап 1: API endpoints
   Этап 2: БД (слишком поздно!)
```

**Правильно:**
```
✅ Этап 1: Database setup
   Этап 2: Domain models  
   Этап 3: API endpoints
```

---

## 4️⃣ Четкие условия завершения

**Правило:** Для каждого этапа есть **конкретные, проверяемые** условия.

**Плохо:**
```
❌ Фаза 5 завершена когда:
  - "Работает"
  - "Тесты пройдены"
  - "Нет ошибок"
```

**Хорошо:**
```
✅ Фаза 5 завершена когда:
  - All unit tests passing (coverage > 80%)
  - Integration tests for happy path passing
  - Code review approved
  - Performance: response time < 200ms
```

---

## 5️⃣ Нет циклических зависимостей

**Правило:** Зависимости должны быть **DAG** (Directed Acyclic Graph).

```
❌ Плохо:     ✅ Хорошо:
  A → B        A → B
  B → C        B → C
  C → A ❌      C (конец)
```

**Проверка:** Можешь ли нарисовать только стрелки вниз? Если да → правильно.

---

## 6️⃣ Single Responsibility

**Правило:** Каждый компонент / сервис / модуль имеет **одну причину для изменения**.

**Плохо:**
```
❌ UserService: управление пользователем + отправка emails + логирование
```

**Хорошо:**
```
✅ UserService: управление пользователем
   EmailService: отправка emails
   LoggingService: логирование
```

---

## 7️⃣ Таблица зависимостей

**Правило:** Каждый план имеет **явную таблицу** зависимостей этапов.

```
| Этап | Зависит от | Условия |
|------|-----------|---------|
| 1    | -         | ... |
| 2    | 1         | ... |
| 3    | 1,2       | ... |
```

**Почему:** Это единственный способ увидеть параллелизм и конфликты.

---

## 8️⃣ Тестируемость каждого этапа

**Правило:** Каждый этап должен быть **тестируемым** сам по себе.

```
Этап 1: Domain models → Unit tests
Этап 2: Database → Integration tests с БД
Этап 3: Services → Unit + Integration tests
Этап 4: API → API tests
```

**Если этап нельзя тестировать** → он слишком большой, разбей его.

---

## 9️⃣ Нет дублирования определений

**Правило:** Каждый компонент определён **ровно один раз**.

```
❌ Плохо:
   Этап 3: UserService определен
   Этап 6: UserService переопределен с новыми методами
   
✅ Хорошо:
   Этап 3: UserService определен полностью
   Этап 6: UserService используется (не переопределяется)
```

---

## 🔟 Документируй архитектурные решения

**Правило:** К плану прилагается документ "Почему эта архитектура?"

```markdown
## Архитектурные решения

### 1. Почему 3-слойная архитектура?
- Разделение concerns
- Тестируемость
- Переиспользуемость

### 2. Почему микросервисы?
- Независимая масштабируемость
- Паралле разработка
- Развертывание отдельно
```

**Это помогает:** объяснить решение, оспорить его, измениться позже.

---

## ✅ Финальная проверка

Перед тем как использовать план, проверь:

- [ ] Каждый этап N независим от N+1+
- [ ] Зависимости только вниз (DAG)
- [ ] Infrastructure на отдельных этапах
- [ ] Условия завершения конкретные
- [ ] Нет циклических зависимостей
- [ ] Каждый компонент - одна ответственность
- [ ] Есть таблица зависимостей
- [ ] Каждый этап тестируем
- [ ] Нет дублирования
- [ ] Есть объяснение решений

**Все пункты зелены?** → Готово к реализации! ✅

---

## 🚀 Что дальше?

👉 [`core/CHECKLIST.md`](CHECKLIST.md) — финальная валидация плана

👉 [`templates/plan-template.md`](../templates/plan-template.md) — начни писать свой план

👉 [`examples/`](../examples/) — посмотри примеры для своего домена

