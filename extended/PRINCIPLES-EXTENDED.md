# Полная версия: 10 Принципов (с подробностями)

**Это углублённая версия** [`core/PRINCIPLES.md`](../core/PRINCIPLES.md). Читай если хочешь понять "почему" за каждым принципом.

---

## 1️⃣ Линейная независимость этапов

### Что это значит?

Каждый этап N:
- Может быть **завершён** самостоятельно
- Может быть **протестирован** без этапов N+1, N+2, ...
- После завершения **не требует рефакторинга** из этапов N+1

### Почему это важно?

```
Без независимости:
  ❌ Этап 2 закончил → но Этап 5 требует изменений в Этап 2
  ❌ Нельзя начать параллельную разработку
  ❌ Много интеграционных сюрпризов в конце

С независимостью:
  ✅ Каждый этап - тёмный ящик для остальных
  ✅ Параллельная разработка работает
  ✅ Интеграция гладкая
```

### Как проверить

```
Для каждого этапа N:
1. Может ли другая команда начать этап N+1 после завершения этапа N?
   → Если нет: этап N неправильный
2. Может ли быть написан тест этапа N без знания о N+1?
   → Если нет: этап N слишком большой
3. После завершения этапа N нужны ли изменения в N-1?
   → Если да: реструктурируй этапы
```

---

## 2️⃣ Разделение слоёв

### Иерархия

```
┌─────────────────────────────────┐
│ API / Controllers               │ (HTTP, WebSocket, CLI)
├─────────────────────────────────┤
│ Application / Services          │ (Use cases, business orchestration)
├─────────────────────────────────┤
│ Domain / Models                 │ (Pure business logic)
├─────────────────────────────────┤
│ Infrastructure                  │ (Database, APIs, file system)
└─────────────────────────────────┘

Стрелки зависимостей ТОЛЬКО ВНИЗ ↓
```

### Почему это важно

```
Без слоёв:
  ❌ Domain знает о HTTP / React / Flask
  ❌ Тестирование domain требует запуска БД
  ❌ Одна строка в фреймворке ломает весь домен

Со слоями:
  ✅ Domain - чистая бизнес-логика
  ✅ Тестирование domain - просто unit тесты
  ✅ Можно менять фреймворк без изменения логики
```

### Пример: что не должно быть в Domain

```typescript
// ❌ ПЛОХО - Domain зависит от фреймворка:
// user.ts (domain)
import { Request, Response } from 'express';
import { DATABASE_URL } from 'process.env';

export class User {
  static create(req: Request) {
    const name = req.body.name; // зависимость от Express!
    db.connect(DATABASE_URL); // зависимость от фреймворка!
  }
}

// ✅ ХОРОШО - Domain не зависит ни от чего:
// domain/user.ts
export interface CreateUserInput {
  name: string;
  email: string;
}

export class User {
  constructor(public name: string, public email: string) {}
  
  static create(input: CreateUserInput): User {
    // Нет никаких зависимостей!
    return new User(input.name, input.email);
  }
}

// application/user-service.ts
import { User, CreateUserInput } from '../domain/user';
import { userRepository } from '../infrastructure/db';

export class UserService {
  async create(input: CreateUserInput): Promise<User> {
    const user = User.create(input); // используем domain
    return userRepository.save(user); // используем infrastructure
  }
}

// api/user-controller.ts
router.post('/users', async (req, res) => {
  const user = await userService.create(req.body); // используем application
  res.json(user);
});
```

---

## 3️⃣ Infrastructure на отдельных этапах

### Что это значит?

ВСЕ shared infrastructure (которая используется везде) выделены в **отдельные, ранние этапы**.

### Типичная ошибка

```
❌ Этап 1: API endpoints
   Этап 2: Business logic
   Этап 3: Database (слишком поздно!)
   Этап 4: Services (ждут database)
   Этап 5: Repositories (наконец используют database)
```

Проблема: Этапы 1, 2, 4 не могут даже начать свою логику.

### Правильно

```
✅ Этап 1: Infrastructure (DB setup, config, logging)
   Этап 2: Domain (чистая логика, не зависит от инфры)
   Этап 3: Infrastructure impl (repositories, поиск DB)
   Этап 4: Services (используют domain + инфру)
   Этап 5: API (используют services)
```

Теперь Этап 2 может быть разработан и протестирован **без БД**.

---

## 4️⃣ Четкие условия завершения

### Проблема с неясными условиями

```
❌ Плохо:
   "Фаза 5 завершена когда: работает"
   → Что значит "работает"? Один тест? Все тесты? Без багов?
   → Когда можно начинать фазу 6? Неясно!
   → Люди по-разному понимают "готово"
```

### Решение: конкретные критерии

```
✅ Хорошо:
   "Фаза 5 завершена когда:
    - All unit tests passing
    - Coverage > 80%
    - Code review from 1 senior approved
    - Integration tests for all happy paths passing
    - No open critical issues"
    
   → Теперь ясно когда "готово"
   → Когда можно начинать фазу 6? СРАЗУ!
   → Нет субъективности
```

### Как писать условия

1. **Тесты** → "all unit tests > 80% coverage"
2. **Качество кода** → "code review approved", "no critical issues"
3. **Performance** → "response time < 200ms"
4. **Интеграция** → "integration tests passing"
5. **Документация** → "API spec updated"

---

## 5️⃣ Нет циклических зависимостей

### Визуально

```
❌ ПЛОХО (цикл):
   Component A → Component B
   Component B → Component C
   Component C → Component A ← цикл!
   
✅ ХОРОШО (DAG):
   Component A → Component B
   Component B → Component C
   Component C → (конец)
```

### Почему это важно

```
С циклами:
  ❌ Нельзя отделить одно от другого
  ❌ Тесты одного требуют другого (бесконечная цепь)
  ❌ Нельзя переиспользовать в другом проекте

Без циклов:
  ✅ Отдельные компоненты независимы
  ✅ Легко тестировать
  ✅ Легко переиспользовать
```

### Как найти цикл

```
Способ 1: нарисовать стрелки
- Если стрелки идут только вниз → OK
- Если стрелка идёт вверх → цикл!

Способ 2: спросить себя
- Может ли Component A существовать без Component B? Да
- Может ли Component B существовать без Component C? Да
- Может ли Component C существовать без Component A? Да
- → OK, нет цикла
```

---

## 6️⃣ Single Responsibility

### Пример: раздутый сервис

```typescript
❌ ПЛОХО:
export class UserService {
  // Управление юзером
  createUser(data) { /* ... */ }
  updateUser(data) { /* ... */ }
  deleteUser(id) { /* ... */ }
  
  // Отправка emails (не то!)
  sendWelcomeEmail(userId) { /* ... */ }
  sendPasswordReset(userId) { /* ... */ }
  
  // Логирование (не то!)
  logUserAction(userId, action) { /* ... */ }
  
  // Уведомления (не то!)
  notifyUser(userId, message) { /* ... */ }
}
```

Эт сервис меняется по 4 разным причинам:
1. Когда меняется логика юзера
2. Когда меняется система emails
3. Когда меняется логирование
4. Когда меняется система уведомлений

### Правильно

```typescript
✅ ХОРОШО:
// Только управление юзером
export class UserService {
  createUser(data) { /* ... */ }
  updateUser(data) { /* ... */ }
  deleteUser(id) { /* ... */ }
}

// Отдельно
export class EmailService {
  sendWelcomeEmail(userId) { /* ... */ }
  sendPasswordReset(userId) { /* ... */ }
}

// Отдельно
export class AuditLogger {
  logUserAction(userId, action) { /* ... */ }
}

// Отдельно
export class NotificationService {
  notifyUser(userId, message) { /* ... */ }
}
```

Теперь каждый сервис меняется только по одной причине.

---

## 7️⃣ Таблица зависимостей

### Почему это важно

Таблица зависимостей - **единственный способ увидеть всю структуру проекта**.

```
Без таблицы:
  ❌ Непонятно что от чего зависит
  ❌ Нельзя спланировать параллельную разработку
  ❌ Сюрпризы в конце: "О, этап X зависит от Y!"

С таблицей:
  ✅ Видна вся структура
  ✅ Ясен параллелизм
  ✅ Нет сюрпризов
```

### Пример

```
| Этап | Зависит от | Условия |
|------|-----------|---------|
| 1    | -         | ... |
| 2    | 1         | ... |
| 3    | 1, 2      | ... |
| 4    | 3         | ... |

Из таблицы видно:
- Этапы 1 и 2 параллельны? Нет (2 ждёт 1)
- Этапы 2 и 3? Нет (3 ждёт обоих)
- Этап 4? Ждёт только 3

Параллелизм:
  Team A: [1] → [2] → [3]
  Team B:   (ждёт) → [3] → [4]
```

---

## 8️⃣ Тестируемость

### Слои тестирования

```
Этап 1 (Infrastructure):
  → Smoke tests (включается ли?)

Этап 2 (Domain):
  → Unit tests (логика правильна?)

Этап 3 (Infrastructure impl):
  → Integration tests с БД (запросы правильны?)

Этап 4 (Services):
  → Unit tests (бизнес логика правильна?)
  → Integration tests с repos (работают вместе?)

Этап 5 (API):
  → Unit tests (контроллеры правильны?)
  → Integration tests (весь flow работает?)
  → E2E tests (юзер может использовать?)
```

### Принцип: каждый этап независимо тестируем

```
❌ Плохо:
   Этап 3 тест требует фреймворка из Этапа 5

✅ Хорошо:
   Этап 3 может быть протестирован без Этапа 5
   (потому что он не зависит от Этапа 5)
```

---

## 9️⃣ Нет дублирования

### Пример

```
❌ Плохо:
   Этап 3: UserService определён
   Этап 6: UserService переопределён с новыми методами
   
   → Какая версия правда? Конфузия!

✅ Хорошо:
   Этап 3: UserService определён полностью
   Этап 6: UserService используется (без изменений)
   
   → Одна версия, ясно!
```

---

## 🔟 Документируй решения

### Зачем?

```
Без документации:
  ❌ "Почему эта архитектура?" - неизвестно
  ❌ "Можно ли изменить?" - неясно
  ❌ Новые люди в команде теряются

С документацией:
  ✅ Историческое обоснование
  ✅ Альтернативы и why we didn't choose them
  ✅ Новые люди быстро разбираются
```

### Что документировать?

```
## Архитектурные решения

### 1. Почему 3-слойная архитектура?
- Причина: разделение concerns
- Альтернатива: монолит (рассматривали, но сложнее тестировать)
- Trade-offs: больше слоёв, но тестируемость выше

### 2. Почему такой порядок этапов?
- Infrastructure first: нужна для остальной логики
- Alternatives: infrastructure last (не работает, не можем тестировать)

### 3. Почему не используем pattern X?
- Рассмотрели, но для нас это overkill
- Добавляет сложность без бенефитов
```

---

## Резюме

Эти 10 принципов - **фундамент хороших планов**.

Если всё соблюдено → план буквально **невозможно сломать** во время реализации.

👉 Перейди к [`core/CHECKLIST.md`](../core/CHECKLIST.md) для финальной валидации.

